import React from "react";
import { Text, View, StyleSheet } from "react-native";

const Receipt = () => {
  return (
    <View
      style={{
        display: "flex",
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>This is the receipt</Text>
    </View>
  );
};

export default Receipt;
